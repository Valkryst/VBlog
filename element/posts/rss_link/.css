x-posts-rss-link > a {
    display: flex;
    align-items: center;
    justify-content: center;

    margin: calc(var(--navigation-height) * 0.25) 0;

    color: var(--color-link-unvisited);
    text-align: center;
    text-decoration: none;

    img {
        max-width: 1rem;

        margin-right: 0.25rem
    }
}