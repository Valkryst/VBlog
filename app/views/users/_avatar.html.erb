<% if current_user.avatar_url.present? %>
  <img id="navigation-bar-user-avatar" src="<%= current_user.avatar_url %>" alt="<%= current_user.name %>'s Avatar'">
<% else %>
  <%= image_tag("icon_user.svg", :alt => "Log In Icon") %>
<% end %>