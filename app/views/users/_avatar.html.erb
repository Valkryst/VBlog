<% if user.avatar_url.present? %>
  <img id="navigation-bar-user-avatar" src="<%= user.avatar_url %>" alt="<%= user.name %>'s Avatar'">
<% else %>
  <%= image_tag("icon_user.svg", :alt => "Log In Icon") %>
<% end %>